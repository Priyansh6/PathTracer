#ifndef TILE_H
#define TILE_H

constexpr int tile_size = 16;
// The Tile class contains coordinates representing the top-left corner of a tile in an image.
struct Tile
{
  int x, y;
};

#endif// TILE_H
